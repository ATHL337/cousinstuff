baseURL = "https://example.com/"
languageCode = "en-us"
title = "CousinStuff"
enableRobotsTXT = true

[taxonomies]
  tag = "tags"

[params]
  description = "Family recipes, shared simply."
  brand = "CousinStuff"
  description = "A family recipe registry you can search, tag, and print."
  ogImage = "/img/og/og-corp.png"

  

[outputs]
  home = ["HTML"]
  section = ["HTML", "JSON"]
  taxonomy = ["HTML"]
  term = ["HTML"]

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = false

enableRobotsTXT = true

[sitemap]
changefreq = "weekly"
priority = 0.5
filename = "sitemap.xml"